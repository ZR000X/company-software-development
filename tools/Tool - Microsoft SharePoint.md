# Microsoft SharePoint

Tool used for hosting the Excel workbook in a centralized location so any Person can run the process. Used in [Workstream ticket update workflow](../processes/workflows/workstream-ticket-update-workflow.md) to host the workbook so any [Person](../entities/person.md) can run the process.
