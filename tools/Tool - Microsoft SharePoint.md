# Microsoft SharePoint

Tool used for hosting the Excel workbook in a centralized location so any Person can run the process. Used in [Workstream management](../processes/workstream-management.md) to host the workbook so any [Person](../entities/person.md) can run the process.
