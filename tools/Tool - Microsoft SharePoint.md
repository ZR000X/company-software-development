# Microsoft SharePoint

Tool used for hosting the Excel workbook in a centralized location so any Person can run the process. Used in [[Periodic Work Management - Task Updates & Alignment across Workers]] to host the workbook so any [[Entity - Person|Person]] can run the process.
